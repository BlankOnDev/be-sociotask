# Example environment file for be-socialtask
# Copy this file to .env and replace the placeholders with real values

# Database configuration (Postgres)
# For Aiven or other providers, adjust sslmode and add sslrootcert if required.
DB_HOST=your-db-host.example.com
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=socialtask
DB_PORT=5432

JWT_SECRET="xxx"

# Optional: override server port when running (main.go defaults to 8080)
# APP_PORT=8080

# Twitter OAuth configuration
TWITTER_CLIENT_ID=your_twitter_client_id
TWITTER_CLIENT_SECRET=your_twitter_client_secret
TWITTER_REDIRECT_URL=your_twitter_redirect_url

# Google Oauth
Google_Client_ID_Web=rahasia
Google_Client_Secret_Web=rahasia
Google_Redirect_URL="https://your-domain/login/google/callback"

# Notes:
# - Do NOT commit real credentials to version control. Add .env to .gitignore.
# - To load this file in a POSIX shell before running the app:
#     set -a; . .env; set +a
# - For PowerShell, set $env:VAR before running `go run .`.
